public interface Shape {
	//requiring that any class we want to be a shake have these methods, but they are implemented differently for each type of shape
	public double getArea();
	public double getPerimeter();
}
